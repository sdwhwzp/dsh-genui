# 2026-09-14 GenUI 渲染修复部署记录

线上版本为 `0.10.0-dsh.20260914.1`，运行代码提交为 `713063588a23a310875250908f054b0d90ce6cc4`，发布分支为 `dev`。

## 问题与修复

历史回答包含正确的 `dsh-ui` 围栏，但 `hero.tone: "brand"` 未通过组件校验，DOM 渲染通道因此保留原始代码。现在将该值规范化为 `accent`；其他无法渲染的内容会在原始代码上方显示具体错误，修正内容后移除错误提示。历史 Session 文件不变。

任务栏显示“1 进行中、4 待处理”的案例中，日志只有初始的一次 `todo/write`，之后已有 `turn/end`，没有后续清单更新。该状态来自模型遗漏任务状态更新，并不表示后台仍在执行；本次没有改写任务状态或将未核实事项自动标为完成。

## 验证与部署

- 4 个针对性测试文件共 81 项测试通过，构建通过。
- 使用线上保存内容进行一次临时回放，6 个围栏均可解析渲染；私有回放内容未提交。
- 服务健康检查返回 200，未认证 Host 请求返回 401，连续 16 次观测覆盖 21.86 秒。
- 部署前后均为 189 个会话、0 个运行中会话；331 个依赖中其余 330 个保持不变，34 行配置补丁完整保留。
- 发布包 SHA-256：`b4d7e8c86facf4b82aeefe6b510d4d31291bde4d497d43bc72227f7327fcd069`。
- 远程发布包：`/home/tzwl3/apps/dsh-plugins/releases/20260914-genui-render/artifacts/changfenhuang-dsh-genui-0.10.0-dsh.20260914.1.tgz`。
- 远程审计目录：`/home/tzwl3/apps/deploy-staging/20260914-genui-render`。
- 回滚配置：`/home/tzwl3/.dsh/profiles/web-before-20260914-genui-render`。

## 清理

远程发布包及已安装文件完成校验后，已删除本次本地临时脚本、日志、私有回放数据及打包文件。本次未创建本地常驻服务或隧道；源码、测试、共享依赖及远程审计和回滚材料保留。
